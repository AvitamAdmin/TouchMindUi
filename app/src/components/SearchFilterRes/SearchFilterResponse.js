import React from "react";

const SearchFilterResponse = (message) => {
  const handleMessage = (message) => {
    console.log("Received message: ", message);
    return <div>SearchFilterResponse</div>;
  };
};

export default SearchFilterResponse;
